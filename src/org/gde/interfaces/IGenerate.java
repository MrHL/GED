package org.gde.interfaces;

/**
 * the interface improve generate method,all generator class
 * implements IGenerate
 * @author JavaLuSir
 *
 */
public interface IGenerate {

	/**
	 * generate method
	 * @param path the path is a String filepath which you want to generate to
	 */
	void generate(String path);
}
